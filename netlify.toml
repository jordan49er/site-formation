[build]
command = "git submodule update --init --recursive && hugo --gc --minify"
publish = "public"

[build.environment]
HUGO_VERSION = "0.156" # Remplacez par votre version (hugo version)

[context.production.environment]
HUGO_ENV = "production"
